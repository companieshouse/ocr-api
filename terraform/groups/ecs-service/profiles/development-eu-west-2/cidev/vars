environment = "cidev"
aws_profile = "development-eu-west-2"
max_task_count = 1
# service configs
log_level = "debug"

# scaling configs
service_scaledown_schedule = "55 19 * * ? *"
service_scaleup_schedule   = "5 6 * * ? *"

required_cpus    = 3840
required_memory  = 15616